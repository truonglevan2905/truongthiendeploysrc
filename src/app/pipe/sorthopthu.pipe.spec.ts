import { SorthopthuPipe } from './sorthopthu.pipe';

describe('SorthopthuPipe', () => {
  it('create an instance', () => {
    const pipe = new SorthopthuPipe();
    expect(pipe).toBeTruthy();
  });
});
